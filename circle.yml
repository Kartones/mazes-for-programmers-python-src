version: 2
jobs:
  build:
    docker:
       - image: circleci/python:3.6
    steps:
       - checkout
       - run:
          name: Install Requirements
          command: pip install -r requirements.txt
        - run:
          name: Run Tests
          command: pytest
